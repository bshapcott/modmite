An Apache module for lightweight sqlite and server side JavaScript.

Project goals:
  * direct access from HTTP to sqlite data, bypassing any scripting layer
  * JavaScript behind the database, in a sqlite virtual table